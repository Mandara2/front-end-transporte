import { RestauranteModel } from './restaurante.model.model';

describe('RestauranteModel', () => {
  it('should create an instance', () => {
    expect(new RestauranteModel()).toBeTruthy();
  });
});
