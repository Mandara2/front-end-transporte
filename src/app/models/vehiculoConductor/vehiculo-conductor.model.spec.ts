import { VehiculoConductor } from './vehiculo-conductor.model';

describe('VehiculoConductor', () => {
  it('should create an instance', () => {
    expect(new VehiculoConductor()).toBeTruthy();
  });
});
