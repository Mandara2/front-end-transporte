import { AdministradorModel } from './administrador.model.model';

describe('AdministradorModel', () => {
  it('should create an instance', () => {
    expect(new AdministradorModel()).toBeTruthy();
  });
});
