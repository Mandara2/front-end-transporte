import { CentroDistribucionModel } from './centro-distribucion.model.model';

describe('CentroDistribucionModel', () => {
  it('should create an instance', () => {
    expect(new CentroDistribucionModel()).toBeTruthy();
  });
});
