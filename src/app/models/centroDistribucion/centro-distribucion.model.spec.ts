import { CentroDistribucion } from './centro-distribucion.model';

describe('CentroDistribucion', () => {
  it('should create an instance', () => {
    expect(new CentroDistribucion()).toBeTruthy();
  });
});
