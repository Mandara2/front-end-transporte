import { DirListaOrdenModel } from './dir-lista-orden.model.model';

describe('DirListaOrdenModel', () => {
  it('should create an instance', () => {
    expect(new DirListaOrdenModel()).toBeTruthy();
  });
});
